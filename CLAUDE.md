# xmemory-ai (Python client) — agent rules

See [AGENTS.md](AGENTS.md) for the agent rules for this repo. In particular:
**always bump the `pyproject.toml` version (semver) and add a `CHANGELOG.md`
entry on any caller-visible change.**
